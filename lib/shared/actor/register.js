import { ActorStore } from './stores/actor_store'
import { ActorActorStore } from './stores/actor_actor_store'
import { ActorTypeStore } from './stores/actor_type_store'
import { ActorFilterStore } from './stores/actor_filter_store'

import { loadActors } from 'lib/shared/actor/actions'

//import { router } from 'laosdirg-flux-router/router'

//------------------------------------------------------------------------------

/*
context.action(addRoute, ROUTE_ACTOR_LIST)

router.get('/:lang/actors', action => {
  action( loadActors, 'MAIN' )
})
router.get('/:lang/actors/:actor', run => {
  run()
})*/

export function register(){

}
